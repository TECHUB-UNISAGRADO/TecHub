import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.unisagrado',
  appName: 'TecHub',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
